# Netlify Configuration for LightOS DCIM Dashboard

[build]
  # Publish directory
  publish = "docs-site"

  # No build command needed for static site
  command = "echo 'Deploying LightOS DCIM Dashboard'"

# Redirect root to DCIM dashboard
[[redirects]]
  from = "/"
  to = "/dcim.html"
  status = 302

# Redirect documentation paths
[[redirects]]
  from = "/docs/*"
  to = "/guides/:splat"
  status = 200

[[redirects]]
  from = "/api-reference/*"
  to = "/api/:splat"
  status = 200

# DCIM API Proxy (existing)
[[redirects]]
  from = "/api/dcim/*"
  to = "http://localhost:8001/api/dcim/:splat"
  status = 200
  force = false

# Antigravity — Context Engine (port 8010)
[[redirects]]
  from = "/api/context/*"
  to = "http://localhost:8010/context/:splat"
  status = 200
  force = false

# Antigravity — Router API (port 8011)
[[redirects]]
  from = "/api/router/*"
  to = "http://localhost:8011/router/:splat"
  status = 200
  force = false

[[redirects]]
  from = "/api/config/*"
  to = "http://localhost:8011/config/:splat"
  status = 200
  force = false

# Antigravity — Inference Proxy (port 8012)
[[redirects]]
  from = "/api/inference/*"
  to = "http://localhost:8012/inference/:splat"
  status = 200
  force = false

# Antigravity — Telemetry API (port 8013)
[[redirects]]
  from = "/api/telemetry/*"
  to = "http://localhost:8013/telemetry/:splat"
  status = 200
  force = false

# WebSocket proxy for real-time telemetry
[[redirects]]
  from = "/ws/dcim"
  to = "ws://localhost:8001/ws/dcim"
  status = 200
  force = false

# Headers for security and CORS
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, POST, PUT, DELETE, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type, Authorization"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Custom 404 page
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

# Settings
[build.environment]
  NODE_VERSION = "18"

# Deploy previews
[context.deploy-preview]
  command = "echo 'Deploy preview'"

# Branch deploys
[context.branch-deploy]
  command = "echo 'Branch deploy'"

# Production settings
[context.production]
  command = "echo 'Production build'"
