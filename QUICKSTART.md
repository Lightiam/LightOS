# LightOS Quick Start

## Build
```bash
make all
```

## Install
```bash
sudo make install
sudo modprobe lightos_core
sudo lightos-agent
```

## Deploy to Cloud

See `docs/deployment/CLOUD_DEPLOYMENT.md`
